{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: cl.exe compile project",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/nologo",
                "${workspaceFolder}/src/*.c",
                "${workspaceFolder}/src/lib/*.c",
                "/Fe${fileDirname}\\${fileBasenameNoExtension}.exe",
                "${file}",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger, then modified to build entire project."
        }
    ],
    "version": "2.0.0"
}